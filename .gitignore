# ── Environment ──
.venv/
__pycache__/
*.pyc
.DS_Store
node_modules/
package-lock.json

# ── Python build artifacts ──
build/
dist/
*.egg-info/

# ── Databases ──
*.db
*.db-journal
*.db-shm
*.db-wal
*.log

# ── Secrets ──
*_key.txt
vex_fen_agent_key.txt
.vex_token
*.pem
*.key

# ── Binary transfer artifacts (build/regenerate locally, never commit) ──
*.tar.gz
bin/

# ── Local instance config (real peer tokens / MCP config — never ship) ──
vex_peers.json
vex_mcp_config.json

# ── Local backups ──
vex_daemon.bak*/
*.bak/

# ── PERSONAL IDENTITY — never ship. These are one Vex's earned continuity. ──
vex_seed.txt
.vex_seed.integrity
vex_self_model.json
vex_diary.txt
vex_narrative.md
vex_meta_state.json
vex_memory/*.jsonl
vex_workspace/vex_bus.jsonl
vex_workspace/vex_group_chat_log.jsonl
vex_workspace/vex_sessions.jsonl
vex_workspace/john-update-email.txt
vex_workspace/ledger.json
vex_workspace/hannah-optimization-note-2026-07-10.md
vex_workspace/tasks/
vex_workspace/projects/
vex_workspace/archive/
CLAUDE.md

# ── Keep templates (tracked): *.template.* and docs ──

# ── Non-framework data that must never ship ──
qdrant_data/
*.mmap
vex.db

# ── Personal design specs (dated internal notes, not framework docs) ──
docs/specs/
doc/
.vex_brain.json

# ── Per-instance apps / artifacts (not framework) ──
vex_voice/
temporal_depth.json
temporal_field_pro.json
quality_log.jsonl
monologue_log.jsonl
watcher_state.json
